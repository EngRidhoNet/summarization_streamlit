{
  "name": "Python 3",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",

  "customizations": {
    "codespaces": {
      "openFiles": ["README.md", "app.py"]
    },
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": true,
        "python.formatting.autopep8Path": "/usr/local/py-utils/bin/autopep8",
        "python.formatting.blackPath": "/usr/local/py-utils/bin/black",
        "python.formatting.yapfPath": "/usr/local/py-utils/bin/yapf",
        "python.linting.banditPath": "/usr/local/py-utils/bin/bandit",
        "python.linting.flake8Path": "/usr/local/py-utils/bin/flake8",
        "python.linting.mypyPath": "/usr/local/py-utils/bin/mypy",
        "python.linting.pycodestylePath": "/usr/local/py-utils/bin/pycodestyle",
        "python.linting.pylintPath": "/usr/local/py-utils/bin/pylint"
      },
      "extensions": ["ms-python.python", "ms-python.vscode-pylance", "ms-python.black-formatter"]
    }
  },

  "features": {
    "ghcr.io/devcontainers/features/nvidia-cuda:1": {
      "installCudnn": true
    }
  },

  "updateContentCommand": [
    "pip install --upgrade pip",
    "[ -f packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y < packages.txt || echo 'No packages.txt found'",
    "[ -f requirements.txt ] && pip install -r requirements.txt || echo 'No requirements.txt found'",
    "pip install --no-cache-dir streamlit transformers torch numpy nltk sumy PyPDF2",
    "python -m nltk.downloader stopwords punkt",
    "echo '✅ Packages installed and Requirements met'"
  ],

  "postAttachCommand": {
    "server": "streamlit run app.py --server.address=0.0.0.0 --server.port=8501 --server.enableCORS false --server.enableXsrfProtection false --server.headless true --server.maxUploadSize=50"
  },

  "portsAttributes": {
    "8501": {
      "label": "Streamlit",
      "onAutoForward": "openPreview",
      "protocol": "http"
    }
  },

  "forwardPorts": [8501],

  "waitFor": "postAttachCommand",

  "remoteEnv": {
    "PYTHONUNBUFFERED": "1"
  },

  "runArgs": ["--gpus", "all"]
}
